name: Release action
# Call the reusable workflow release-solution-with-inputs.yml
# Release your solution to prod when you create a new release.

on:
  release:
      types: [created]        

jobs:
  Release-solution-ALMLab:
    uses: ./.github/workflows/release-solution-with-inputs.yml
    with:
      #You can specify the solution name here
      solution_name: ALMLab
      #Update your values here
      BUILD_ENVIRONMENT_URL: 'https://orga259a2b4.crm2.dynamics.com'
      PRODUCTION_ENVIRONMENT_URL: 'https://org86768755.crm2.dynamics.com/'
      USERNAMEDESTINO: 'admin@almdestino.onmicrosoft.com'
      USERNAMEORIGEM: 'admin@almorigem.onmicrosoft.com'
      TENANT_ID: '0294b233-b846-4dbb-b190-b83fe3643c0d'      
    secrets:
      envSecret: ${{ secrets.PASSWORD }}
